<Window x:Class="KaraokeMakerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KaraokeMakerWPF"
        xmlns:components="clr-namespace:KaraokeMakerWPF.Components.StepByStepControl.Views;assembly=KaraokeMakerWPF.Components"
        xmlns:views="clr-namespace:KaraokeMakerWPF.Views"
        xmlns:models="clr-namespace:KaraokeMakerWPF.ViewModels"
        d:DataContext="{d:DesignInstance Type=models:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="Создатель Караоке"
        Height="750"
        MinHeight="500"
        Width="1200"
        MinWidth="820">
    <Window.DataContext>
        <models:MainWindowViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="120"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>

        <components:StepByStep DataContext="{Binding StepByStepVM}" Margin="20"/>

        <Grid Grid.Row="1" DataContext="{Binding StepByStepViewModel}">
            <Grid.Resources>
                <DataTemplate DataType="{x:Type models:SelectBackgroundViewModel}">
                    <views:SelectBackground DataContext="{Binding}" VerticalAlignment="Top"/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type models:SelectFontViewModel}">
                    <views:SelectFont DataContext="{Binding}" VerticalAlignment="Top"/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type models:SelectMusicViewModel}">
                    <views:SelectMusic DataContext="{Binding}" VerticalAlignment="Top"/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type models:SelectSongTextViewModel}">
                    <views:SelectSongText DataContext="{Binding}" VerticalAlignment="Top"/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type models:CreateSongMarkupViewModel}">
                    <views:CreateSongMarkup DataContext="{Binding}" VerticalAlignment="Top"/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type models:KaraokePreviewViewModel}">
                    <views:KaraokePreview DataContext="{Binding}" VerticalAlignment="Top"/>
                </DataTemplate>

                <DataTemplate DataType="{x:Type models:CreateKaraokeViewModel}">
                    <views:CreateKaraoke DataContext="{Binding}" VerticalAlignment="Top"/>
                </DataTemplate>
            </Grid.Resources>

            <ContentPresenter Content="{Binding}"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button x:Name="PreviousStepBtn" Width="150" Command="{Binding PreviousStepCommand}">Предыдущий шаг</Button>
            <Button Width="150" Margin="10,0,0,0" Command="{Binding NextStepCommand}">Следующий шаг</Button>
        </StackPanel>
    </Grid>
</Window>
