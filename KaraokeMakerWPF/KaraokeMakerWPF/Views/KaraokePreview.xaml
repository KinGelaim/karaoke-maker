<UserControl x:Class="KaraokeMakerWPF.Views.KaraokePreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KaraokeMakerWPF.Views"
             xmlns:viewmodels="clr-namespace:KaraokeMakerWPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:KaraokePreviewViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="KaraokePreviewRoot">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Content="Путь к фону:"/>
        <Label Grid.Row="0" Content="{Binding KaraokeInfoVM.ImageFilePath}" Margin="150,0,0,0"/>

        <Label Grid.Row="1" Content="Путь к файлу музыки:"/>
        <Label Grid.Row="1" Content="{Binding KaraokeInfoVM.MusicFilePath}" Margin="150,0,0,0"/>

        <Label Grid.Row="2" Content="Путь к шрифту:"/>
        <Label Grid.Row="2" Content="{Binding KaraokeInfoVM.FontFilePath}" Margin="150,0,0,0"/>

        <Grid Grid.Row="3">
            <Image Source="{Binding KaraokeInfoVM.ImageFilePath}" Width="800" Height="Auto"/>
            <TextBlock
                Text="{Binding SongLabelText}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Foreground="White"
                FontSize="24"
            />
        </Grid>

        <Button Grid.Row="4" HorizontalAlignment="Left" Width="150" Command="{Binding StartCommand}">Старт</Button>
        <Button Grid.Row="4" HorizontalAlignment="Left" Width="150" Margin="170,0,0,0" Command="{Binding StopCommand}">Стоп</Button>
    </Grid>
</UserControl>
