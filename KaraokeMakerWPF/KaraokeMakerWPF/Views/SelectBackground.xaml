<UserControl x:Class="KaraokeMakerWPF.Views.SelectBackground"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KaraokeMakerWPF.Views"
             xmlns:viewmodels="clr-namespace:KaraokeMakerWPF.ViewModels"
             xmlns:converters="clr-namespace:KaraokeMakerWPF.Converters"
             d:DataContext="{d:DesignInstance Type=viewmodels:SelectBackgroundViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:StringToBitmapImageConverter x:Key="StringToBitmapImageConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Button
            Content="Выбрать изображение"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Height="30"
            Width="200"
            Command="{Binding SelectImagePathCommand}"
        />
        <Image
            Source="{Binding KaraokeInfoVM.ImageFilePath, Converter={StaticResource StringToBitmapImageConverter}}"
            Margin="0,40,0,0"
            Width="800"
        />
        <ComboBox
            ItemsSource="{Binding Images}"
            SelectedValue="{Binding KaraokeInfoVM.ImageFilePath}"
            SelectedValuePath="ImagePath"
            DisplayMemberPath="DisplayText"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Width="250"
        />
    </Grid>
</UserControl>
